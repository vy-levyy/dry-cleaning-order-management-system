import React from 'react';
import PageWrapper from '../PageWrapper';
import FormCleaner from '../../components/forms/FormAddCleaner';

const PageAddCleaner: React.FunctionComponent = () => {
  return (
    <PageWrapper>
      <FormCleaner />
    </PageWrapper>
  );
}

export default PageAddCleaner;
